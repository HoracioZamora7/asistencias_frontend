import { RelojPipePipe } from './reloj-pipe.pipe';

describe('RelojPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RelojPipePipe();
    expect(pipe).toBeTruthy();
  });
});
